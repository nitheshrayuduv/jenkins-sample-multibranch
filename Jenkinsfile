pipeline {
    agent any

    stages {
        stage('Wait') {
            steps {
                script {
                    // Wait for 30 seconds
                    sleep(time: 30, unit: 'SECONDS')
                }
            }
        }
    }
}
